{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Package",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceRoot}",
      "args": ["noise.wav", "output.raw"],
      "env": {
        "LD_LIBRARY_PATH": "${workspaceFolder}/.libs:${env:LD_LIBRARY_PATH}"
      }
    }
  ]
}
